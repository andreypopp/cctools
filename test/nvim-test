#!/usr/bin/env bash
set -eu

# Launch Neovim with cctools test environment
# Usage: ./test/nvim-test [file...]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$REPO_DIR"
exec nvim -u test/init.lua "$@"
